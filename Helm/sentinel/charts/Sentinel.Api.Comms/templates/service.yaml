apiVersion: v1
kind: Service
metadata:
  name: sentinel-api-comms-helm
  labels:
    app: sentinel-api-comms-helm
spec:
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  selector:
    app: sentinel-api-comms-helm
