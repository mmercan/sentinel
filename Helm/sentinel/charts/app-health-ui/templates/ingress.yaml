{{- $host := printf "%s.%s.%s" "health" .Values.environment .Values.hostdomain -}}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name:  {{ include "Sentinel.UI.HealthMonitoring.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "Sentinel.UI.HealthMonitoring.name" . }}
    helm.sh/chart: {{ include "Sentinel.UI.HealthMonitoring.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    kubernetes.io/ingress.class: nginx
    certmanager.k8s.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  tls:
  - hosts:
    - {{ $host }}
    secretName: "{{ $host }}-secret"
  rules:
  - host: {{ $host }}
    http:
      paths:
      - path: /
        backend:
          serviceName: {{ include "Sentinel.UI.HealthMonitoring.fullname" . }}
          servicePort: 80