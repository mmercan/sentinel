apiVersion: v1
kind: Service
metadata:
  name: {{ include "Sentinel.Util.Mailhog.fullname" . }}
  labels:
    {{- include "Sentinel.Api.Mailhog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Sentinel.Api.Mailhog.selectorLabels" . | nindent 4 }}
